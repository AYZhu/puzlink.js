# puzlink.js

[puzlink](https://github.com/cosmologicon/puzlink) but it can run in your browser.

### example
example serves the root folder and is not intended to be used in production; to run, install `http-server`, then run `npm run build:dev` and `npm run dev`.